import { TerminalExecutor } from "../types";
/**
 * Running command from cmd.
 *
 * **NOTE:** CMD terminal process will start with `{ shell: true }` **SpawnOption**.
 */
declare const runCmd: TerminalExecutor;
export default runCmd;
